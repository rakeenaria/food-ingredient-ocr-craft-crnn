# Outputs Folder

Canonical output location for OCR runs.

- `outputs/crops/`: cropped line/word images.
- `outputs/craft/`: recognized text, merged text, heatmaps, overlays.
- `outputs/examples/`: curated sample artifacts for README/demo.

Legacy output folders (`craft_crops`, `craft_results`) are still supported via CLI arguments.
