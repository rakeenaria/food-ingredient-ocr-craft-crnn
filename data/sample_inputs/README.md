# Sample Inputs

Canonical location for small demo input images.

Compatibility note:
- Existing repository commands still use `./bahan` by default.
- You can also run with `--input_folder data/sample_inputs`.
