# Scripts Folder

Runnable entrypoints.

- `run_demo.py`: canonical OCR inference launcher.
- `../app_streamlit.py`: local Streamlit UI launcher (`streamlit run app_streamlit.py`).

Compatibility note:
- `python demo.py ...` remains fully supported and forwards to the same pipeline.
